import { View } from "react-native";

// This is a placeholder for the FAB button in the tab bar.
// Navigation to this screen is prevented by the tabPress listener in _layout.tsx
export default function ActionPlaceholder() {
    return <View />;
}
